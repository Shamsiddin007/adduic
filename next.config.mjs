// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
  // bu yerda konfiguratsiya sozlamalaringizni kiriting
  turbo: true, // Turbopack o'chiriladi va Webpack ishlatiladi
};

export default nextConfig;
